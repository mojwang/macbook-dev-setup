# API Keys and Tokens
# This file is managed by the setup scripts
# Manual edits are preserved

# MCP Server API Keys
export EXA_API_KEY="${EXA_API_KEY:-}"
export FIGMA_API_KEY="${FIGMA_API_KEY:-}"

# Other API Keys
export ANTHROPIC_API_KEY="${ANTHROPIC_API_KEY:-}"
export OPENAI_API_KEY="${OPENAI_API_KEY:-}"
export GITHUB_TOKEN="${GITHUB_TOKEN:-}"

# GitHub Corporate Organization (auto-detected by setup, used by ghrepo function)
export GH_CORP_ORG="${GH_CORP_ORG:-}"