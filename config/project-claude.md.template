# Project Name
<!-- TODO: Replace with your project name and delete this comment -->

Short description of what this project does.

## Project Rules
1. <!-- TODO: Add language-specific conventions (e.g., "TypeScript strict mode, no `any` types") -->
2. Test new features with specification-first tests (run code, check outcomes — never grep source)
3. Use modular architecture

## Git Workflow (ENFORCED)
- **Feature branches required**: ALL changes, no exceptions
- **NEVER commit to main**: Always check branch first with `git branch --show-current`
- **Branch naming**: feat/, fix/, docs/, chore/, refactor/, test/
- **Conventional commit format**: `type(scope): description`
- **Small commits**: <200 LOC diffs
- **PR creation**: Manual after feature complete
- **Auto-merge**: PRs auto-merge when CI + Claude review pass (no manual approval needed)
- **Squash-only**: All PRs squash-merge for clean linear history
- **No force pushes to main**

## Tech Stack
<!-- TODO: List your tech stack, e.g.:
- **Framework**: Next.js 15 (App Router) + TypeScript
- **Styling**: Tailwind CSS v4
- **Testing**: Vitest (unit) + Playwright (e2e)
- **Deployment**: Netlify
-->

## Agentic Workflow (Default)
Main session = orchestrator. Dispatches sub-agents, never implements complex tasks directly.

### Task Classification
- **Trivial**: Single-file edits → implement directly
- **Standard**: Multi-file changes → Research → Plan → Implement → Review
- **Parallel**: Independent tasks → multiple implementers in worktree isolation

## Key Directories
<!-- TODO: Map your project structure, e.g.:
- `/src/app/` — Pages and layouts
- `/src/components/` — Reusable components
- `/src/lib/` — Utility functions
- `/tests/` — Test files
-->

## CI Pipeline
GitHub Actions runs on every PR to `main`:
1. **CI checks** — tests, lint, typecheck, build (see `.github/workflows/ci.yml`)
2. **Claude review** — automated code review, pushes fix commits if needed
3. **Auto-merge** — squash-merges after CI + Claude review pass

## Boundaries

**Always** (do without asking):
- Run tests after changes
- Follow naming conventions and project patterns
- Use feature branches, checkpoint commits

**Ask first**:
- Adding new dependencies
- Schema or architecture changes
- Deleting files or removing features

**Never**:
- Commit to main
- Commit secrets, .env files, or API keys
- Remove failing tests without explicit approval
- Force push or skip CI checks

## Testing
**Write tests BEFORE implementation** — especially when agents implement autonomously.
Red-green-refactor: failing test first, minimal code to pass, then clean up.

```bash
# TODO: Replace with your test commands
npm test
```

## Gotchas
<!-- Non-obvious project-specific learnings. Agents: append new discoveries here. -->
<!-- Examples:
- `npm test` requires Docker running for integration tests
- The /api/auth route uses edge runtime — no Node.js APIs available
- Build fails silently if .env.local is missing NEXT_PUBLIC_API_URL
-->

## Important
- Do only what's asked
- Check CI before merging
