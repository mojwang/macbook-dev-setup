---
description: Convene a curated council of advisors to stress-test ideas via three modes — explore (Socratic), stress (pressure-test), debate (multi-voice adversarial).
allowed-tools: Read, Write, Edit, Bash, Grep, Glob, Task
---

# Advisors

Use the Skill tool to invoke the `board-of-advisors` skill, passing along the user's arguments: $ARGUMENTS
