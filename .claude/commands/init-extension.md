---
description: Scaffold a new macbook-dev-setup extension pack with install.sh, profile.conf, CI, tests, and CLAUDE.md.
allowed-tools: Bash, Read
---

# Init Extension

Use the Skill tool to invoke the `init-extension` skill, passing along the user's arguments: $ARGUMENTS
